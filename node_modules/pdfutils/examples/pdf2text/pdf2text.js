var pdfutils = require("../../index").pdfutils;

var file = process.argv[2];
if(file === undefined) {
	console.log("Usage: " + process[0] + " " + process[1] + " <pdf file>")
	return;
}

pdfutils(file, function(err, doc) {

	console.dir(doc);
	//console.dir(doc[0].asPDF().toFile('foo.pdf'));

	/*for(var i = 0; i < doc.length; i++) {
		setTimeout(function() {
		doc[1].asText()//.pipe(process.stdout);/
		doc[1].asPNG()//.pipe(process.stdout);/
		doc[1].asSVG()//.pipe(process.stdout);/
		}, i*100);
	}*/
	//for(var i = 0; i < doc.length; i++) {
	//	doc[i].asSVG();//.pipe(process.stderr);
	//}
});
